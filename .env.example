# PostgreSQL Configuration
POSTGRES_PASSWORD=kestra

# Kestra Basic Auth (required for API/MCP access)
# Password must have: 8+ chars, uppercase, lowercase, number
KESTRA_USERNAME=admin@kestra.io
KESTRA_PASSWORD=Kestra2024

# Gemini AI Copilot (for Kestra UI AI features)
GEMINI_API_KEY=your_gemini_api_key_here

# ─── Project-specific variables ──────────────────────────────────
# Add your custom env vars below. Then map them in docker-compose.yml:
#   ENV_my_var: ${MY_VAR}
# Access in flows as: {{ envs.my_var }}
#
# MY_API_TOKEN=your_token_here
# PROXY_URL=
# ─────────────────────────────────────────────────────────────────
